options:
	-h: this help menu

commands:
	all <package>: edit version, clean, build, push, pull (a typical flow after edits to a python package)
	build: build a pypi project to python package format
	clean: remove all unecessary files
	init: initialize a project directory
	pull <package>: upgrade version of local package
	push: update version of package on remote pypi
	setup: pulls new dependencies for pypi on this machine
